- breadcrumb_for_application @application, I18n.t('Disable Scaling')
- content_for :page_title, I18n.t('Disable Scaling')

%h1= I18n.t('Disable Scaling')

%section
  %p= I18n.t(:scale_delete_caption_1, default: 'Your application is currently set to scale the web cartridge. At this time applications that scale cannot have scaling disabled. You must create a new application and migrate your source code if you wish to remove the high availability proxy cartridge that provides scaling.')
